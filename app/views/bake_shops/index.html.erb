<h1>Bake Shops!</h1>

<div class="list-group list-group-flush">
<% @bake_shops.each do |bake_shop| %>
    <%= link_to bake_shop.name, bake_shop_path(bake_shop), class: "list-group-item list-group-item-action" %>

<% end %>
</div>

