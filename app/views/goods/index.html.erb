<h1>Goods' List</h1>

<div>
    <%= button_to "New", new_good_path, method: "get" %>
</div>

<ul>
    <% @goods.each do |good| %>
        <li><%= link_to good.name, good_path(good) %></li>
    <% end %>
</ul>